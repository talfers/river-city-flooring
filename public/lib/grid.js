/*const widths = []
window.onload = function(){
  const projects = document.querySelectorAll('.project');

  projects.forEach(project => {
  const width = Math.floor((Math.random() * 400) + 200) + "px";
  project.style.width = width;
})
}
*/
